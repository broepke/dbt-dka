version: 2

models:
  - name: hubspot__deal
    columns:
      - name: deal_id
        tests:
          - unique
          - not_null
  - name: hubspot__contact
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: contact_id
        tests:
          - unique
          - not_null